import React from "react";

// const delay = (e) => new Promise((resolve) => setTimeout(resolve, e));

export default async function DashboardPage() {
  // await delay(3000); 

  return (
    <div>
      <header>
        <h1 className="text-3xl font-bold text-slate-900">Dashboard Overview</h1>
      </header>
    </div>
  );
}